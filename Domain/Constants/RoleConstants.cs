namespace ClubManagement.Domain.Constants
{
    // Provides constant string values for system roles to avoid magic strings.
    public static class RoleConstants
    {
        // Administrator role with full access permissions.
        public const string Admin = "Admin";

        // Standard member role with limited access permissions.
        public const string Member = "Member";
    }
}
